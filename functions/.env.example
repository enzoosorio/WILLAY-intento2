# Variables consumidas por las Cloud Functions.
# Copiar a functions/.env (NO commitear) o configurar en Secret Manager para deploy.

# Opcional: sin esta key, el fallback Gemini degrada limpiamente a P3 con
# priorityReason="gemini:no_api_key" — la app sigue funcionando.
GEMINI_API_KEY=
